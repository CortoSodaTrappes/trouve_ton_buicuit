{% extends 'test.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    {% if message is defined %}
        {{message}}
    {% endif %}
{% endblock %}