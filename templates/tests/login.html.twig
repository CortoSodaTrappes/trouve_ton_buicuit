{% extends 'test.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    {% if error is defined %}
        {{error}}
    {% endif %}
{% endblock %}